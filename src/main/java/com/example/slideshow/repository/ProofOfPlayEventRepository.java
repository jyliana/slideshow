package com.example.slideshow.repository;

import com.example.slideshow.entity.ProofOfPlayEvent;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProofOfPlayEventRepository extends JpaRepository<ProofOfPlayEvent, Long> {
}
